# Versions below were installed together and smoke-tested (Python 3.12).
# torch is installed separately from the CUDA wheel index, see scripts/setup_env.sh.
transformers==5.17.0
peft==0.21.0
accelerate
datasets==5.0.1
safetensors
omegaconf==2.3.1
pandas==3.0.6
matplotlib
pytest
# scispaCy's en_core_sci_sm requires spaCy < 3.8, which requires numpy < 2;
# scipy/scikit-learn must then stay on releases that still support numpy 1.26.
numpy>=1.26,<2
scipy>=1.11,<1.15
scikit-learn>=1.4,<1.6
spacy>=3.7.4,<3.8
en_core_sci_sm @ https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_sm-0.5.4.tar.gz
